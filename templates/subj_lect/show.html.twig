{% extends 'base.html.twig' %}

{% block title %}SubjLect{% endblock %}

{% block body %}
    <h1>SubjLect</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subj_lect.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subj_lect_index') }}">back to list</a>

    <a href="{{ path('subj_lect_edit', {'id': subj_lect.id}) }}">edit</a>

    {{ include('subj_lect/_delete_form.html.twig') }}
{% endblock %}
