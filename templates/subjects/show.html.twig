{% extends 'base.html.twig' %}

{% block title %}Subjects{% endblock %}

{% block body %}
    <h1>Subjects</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subject.id }}</td>
            </tr>
            <tr>
                <th>Subj_name</th>
                <td>{{ subject.subjName }}</td>
            </tr>
            <tr>
                <th>Hour</th>
                <td>{{ subject.hour }}</td>
            </tr>
            <tr>
                <th>Kurs</th>
                <td>{{ subject.kurs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subjects_index') }}">back to list</a>

    <a href="{{ path('subjects_edit', {'id': subject.id}) }}">edit</a>

    {{ include('subjects/_delete_form.html.twig') }}
{% endblock %}
